# General Store
## Features

- Uses MYSQL as a backend
- Stores data taken (customer data, employee data, stock data) from user directly to MYSQL
- Retrieve Data from MYSQL directly to Python Terminal
- Displays data in Tabular Format (DataFrame)
- Generates Charts(Pie chart, bar chart) from the data given

Made with Python and MYSQL
